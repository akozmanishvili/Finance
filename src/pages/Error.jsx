const Error = () => {
  return (
    <h2
      style={{
        textAlign: "center",
        fontSize: "2rem",
        fontFamily: "sans-serif",
        fontWeight: "bold",
        color: "darkred",
      }}
    >
      Error 404
    </h2>
  );
};

export default Error;
